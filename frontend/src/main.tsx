import React from "react";
import ReactDOM from "react-dom/client";
import "./theme.css";

// Plan 03 will replace this with `import App from "./App"; ... <App />`.
ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <div>Jasper — bootstrap</div>
  </React.StrictMode>,
);
